#include <iostream>
#include "mat.hpp"
using namespace std;
using namespace ariel;

string first = mat(7, 9, '$', '!');
cout << first;

string second = mat(4, 4, '#', '*');
cout << second;

string third = mat(17, 11, '<', '-');
cout << third;
